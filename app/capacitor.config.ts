import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.sapher.bleapp",
  appName: "app",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
